Hadoop Task1 need to be completed:

Task1 : Input can be any format like `text, pdf, xml, json`

Partition the given data based on `Country` and `Status`

Output can be any format like `text, pdf, xml, json`